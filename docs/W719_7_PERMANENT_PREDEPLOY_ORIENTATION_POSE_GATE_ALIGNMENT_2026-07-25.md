# W719.7 — permanent predeploy Orientation-pose gate alignment

## Exact-environment finding

The W719.6 permanent predeploy advanced through the HUD and Project Atlas repairs and stopped in the Babylon-dependent W660J/W661E City touch-runtime gate.

The exact W660J report passed 16 of 17 checks. Its real pointer lifecycle, productive menu entry, accessible authored realm wayfinding and complete six-realm locate/review/enter/return cycle all passed. The only failure was a historical source snapshot that expected a literal `{ x: 0, z: 5.35 }` Orientation spawn.

The maintained City runtime now derives Orientation Hall from the authored W689 pose authority, with W688 and W675 fallbacks, then derives the spawn from that pose's arrival coordinates. This is stronger than duplicating the coordinates in the progressive core.

## Repair

Only the historical W660J source assertion is aligned to require:

- the W689 → W688 → W675 authored Orientation Hall pose chain;
- `ORIENTATION_SPAWN` derived from `ORIENTATION_POSE.arrival`;
- the existing Living Nexus runtime, destination, Connected Core, realm-portal and one-canonical-scene authorities.

No runtime source, spawn coordinates, touch behavior, realm lifecycle, dependency, package lock or production surface changed.
