# W719.2 exact-environment red-team alignment — 2026-07-25

## Exact runner finding

The healthy Node 22 / unchanged-lockfile runner advanced through the W719.1 Babylon residency repair and the W701–W717 institutional gates. In maintained-suite chunk 9 it found one superseded W613 source assertion: the test still required the historical `openEonSharePopover` City integration.

The current City source intentionally replaced that compact popover with the W624J review-first Sharing Center. `data-eon-play-share-city` is bound through `bindEonCitySharingCenter`; the user must prepare a finite public manifest, review included and excluded fields, and then choose a separate native-share, copy, or download action. No publication, referral mutation, reward mutation, tracking, or private-work transfer occurs automatically.

## Repair boundary

- Updated only `tests/unit/w613-eon-city-final-red-team.test.mjs`.
- The test now requires `bindEonCitySharingCenter` and the active `bindCityShare` wiring.
- The test also confirms the superseded `openEonSharePopover` integration is absent from the City station.
- No runtime source, Babylon source, package version, package lock, Cloudflare configuration, secret, or production state changed.

This is a certification-alignment repair, not a product-behaviour change.
