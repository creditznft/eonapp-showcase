# Codex continuation — W358–W365 only

Start from the current cumulative local-first source with W301–W357 applied.

## Non-negotiable boundaries

- No Google Login, One Tap, browser session attachment, account scraping, password/cookie capture, OAuth, social publishing, cloud workspace database, Cloudflare control plane, remote telemetry, crypto/NFT sale, EONLite, wallet, token, payout, referral activation, payment activation, checkout, price, subscription, licence issuance, or deployment.
- Do not revive retired routes, `platform-backend`, legacy social publisher, legacy agent executor, old rewards/referral stack, or existing Polygon contracts.
- Do not fabricate device, external evidence, payment, release, or launch proof.

## Current source facts

- W353–W356 add a local Beta/Release Desk but it cannot invite, enroll, certify, deploy, or activate commerce/referrals.
- W357 reconciles provider protocol aliases and blocks stale model-manifest routing until the user explicitly refreshes metadata.
- `npm run qa:w357-w356-regression` is the cumulative source regression command.
- The inherited full suite has known release-evidence/archive provenance failures. Do not “fix” them by generating substitute evidence or replacing archived hashes.

## W358: Device evidence execution (external hardware only)

Use the Workspace Device Proof Kit on real hardware. Record only user-written pass/fail/blocked status and bounded non-sensitive notes. Keep raw screenshots/user content/private provider payloads out of source and do not claim any result without the actual run.

## W359: Merchant application readiness (external owner work only)

Deploy only the truthful static site when the W301 evidence/history blockers are resolved. Apply for processor test mode using the actual product description: privacy-first AI productivity and creator-workflow software with future personal-use software memberships and packs. Do not describe crypto, NFT marketplace, token rewards, investment, gaming, wallet, referral income, or financial service.

## W360: Payment architecture decision gate (no code until credentials)

Require written merchant approval, one selected processor, a refund/cancellation policy, support owner, webhook/signature design, billing-data allowlist, deletion/reconciliation plan, and test credentials. Then create a separate reviewed branch for hosted checkout only. Browser never handles raw card data or trusts client-only payment success.

## W361: Controlled invite-only beta

Only after W358 evidence and W301 recovery are real. No automatic enrollment, telemetry, commercial entitlement, referral, or payment logic. Use voluntary user-owned support exports.

## W362–W365: Certification and launch board

Run independent security, accessibility, privacy, performance, device, backup/restore, and public-copy review. Require successful full regression, canonical evidence recovery, release board sign-off, rollback plan, and named owners. No public launch if any blocker remains.

## Required validation for every change

- `npm ci --include=dev --no-audit --no-fund`
- `npm run qa:w357-w356-regression`
- targeted new gates/tests
- `npm run lint -- --max-warnings=0`
- `node scripts/secret-scan.mjs --mode=workspace --allow-no-history`
- `npm run build`
- preserve raw output and record any environment limitation honestly.
