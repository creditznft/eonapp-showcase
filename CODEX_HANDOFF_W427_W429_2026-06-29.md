This file is historical-only.
Use `00_START_HERE_W524_MACHINE_VERIFICATION_2026-07-03.md` as the single top-level coding entrypoint.

# Codex Handoff — W427–W429 Canonical City Checkpoint

Use this source bundle as the continuation baseline. Preserve the source-only truth boundaries below.

## Merge target

Implement and review **only W427–W429** from this checkpoint:

- W427: direct Babylon boot diagnostics and same-route safe recovery.
- W428: one canonical public `/eoncity`; aliases redirect but are not active product destinations.
- W429: functional seven-station Command Deck, local/review-first, with explicit native-route confirmation.

## Do not regress

- `/eoncity` remains the sole public City destination.
- Do not reintroduce `/eoncity/lite`, `/eoncity/tour`, `/eoncity/3d`, or `/eoncity/play` into active menus, command routing, service-worker cache targets, or EONBOT route suggestions.
- Do not turn diagnostics into telemetry or persist raw error data, prompts, projects, credentials, or account tokens.
- Do not make Command Deck cards fake monitors. In-world panels must be functional local panels; native routes require a second user confirmation.
- Keep Sync, notifications, rewards, referrals, payments, marketplace/trading, auto-posting, and external execution inactive unless their independent proof gates are passed.

## Required commands

```bash
npm ci
npm run lint -- --max-warnings=0
npm run qa:w392-direct-eoncity-entry
npm run qa:w405-live-ux-city-rescue
npm run qa:w426-city-motion-progression
npm run qa:w427-babylon-direct-boot
npm run qa:w428-one-public-city
npm run qa:w429-functional-command-deck
npm run test:unit
npm run build
npm run smoke:build
npm run audit:site
npm run launch:readiness
node scripts/secret-scan.mjs --mode=workspace --allow-no-history
```

## Merge and deployment review

1. Deploy through the existing normal Cloudflare route only after the command set is green.
2. Manually verify `/eoncity`, each legacy redirect alias, guest Home/Chat, and Command Deck station routes in a real browser.
3. Check service-worker update and cached legacy-route behavior on a device with an earlier build before claiming W428 production proof.
4. Record a raw Lighthouse report only when the runner navigates to the actual route; a `chrome-error-final-url` result is an environment failure, not a score.
5. Do not begin W430 binary/GLTF art work without rights/provenance, LOD, texture-budget, and device review evidence.

## Known non-blocking source caveat

`npm audit --omit=dev` returned no production dependency findings in this environment. Full `npm audit` still reports six development/build-toolchain findings; resolve or formally accept them before any institutional security-release claim.
