# Start Here After W407

Use this source as the only continuation baseline.

## Verify first

```bash
npm ci
npm run verify:w407-arrival-district
```

If the one-line verifier exceeds an outer execution timeout, run each listed
command separately and record real outcomes. Never represent a partial command
as a completed check.

## Product truth now

- Guest-first Google sign-in and compact shell modals are source/build-ready;
  real OAuth proof is still pending.
- Voice uses the single 11-language matrix with visible local browser fallback.
- Sync remains a non-active local schema and migration preview only.
- Share-2 remains local browser-session handoff only; no tracking/posting/reward.
- W406B has zero binary City assets but enforces future art release rules.
- W407 improves Babylon's first-frame Arrival District using original
  procedural geometry. It is **not** final art or device visual proof.

## Next order

1. Capture W406A manual proof: production OAuth test, `/realm` redirect/caches,
   desktop keyboard/mouse and real mobile touch/HUD/reduced-motion City controls.
2. W408: add Creator Atrium and Forge Bay with native routes that remain user
   selected and review-gated.
3. W409: ambient life/quality governor only after W408 clarity is demonstrable.
4. Do not activate EON Sync, Vault Sync, Relay tracking/rewards, social OAuth,
   publishing, payment or Action Gateway.
