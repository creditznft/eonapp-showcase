# Intentional exclusions

This continuation bundle excludes obsolete or irrelevant historical material so the next ChatGPT session stays focused on the current mainstream, non-blockchain product direction.

Excluded:

- `.git`, `node_modules`, `dist`, `.wrangler`, `.env*`, local browser profiles and generated reports;
- local IPNS configuration and historical blockchain/IPFS contract evidence;
- Smart Contracts and retired platform-backend contract artifacts;
- an old saved chat-session transcript;
- temporary screenshots/logs and generated browser test outputs.

The app source still contains some historic route/files for compatibility. The next session must not reactivate retired marketplace, wallet, token, NFT, crypto, or contract functionality.
