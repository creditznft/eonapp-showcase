# EONAPP W624C — Command District Vertical Slice

Date: 2026-07-11  
Status: source-complete; real-runtime visual score and owner/device approval pending  
Prerequisites preserved: W624A Productive Nocturne art bible and W624B runtime consolidation

## Result

W624C builds one finite, reviewable Command District slice without expanding final-quality production across the rest of EON City.

The slice now has:

- one authoritative Arrival Plaza spawn and hero-view contract;
- four explicit first-ten-second cues;
- five first-sixty-second guided milestones;
- seven authored walkable path branches;
- seven collision volumes around the productive landmark route;
- six nearest-safe-point unstuck recoveries;
- six review-first productive destinations;
- one dedicated Agent Theatre exterior with a dormant/review-only truth boundary;
- authored plaza, path, lamp, sign and landmark geometry integrated into the real Babylon scene;
- corrected legacy collision cores so the certified route matches the visible metropolis layout.

## Six productive destinations

| Destination | Canonical route | Honest boundary |
|---|---|---|
| Agent Theatre | `/automations` | Opens automation review; never claims an agent is running without a real receipt |
| Creator Atrium | `/create` | Opens the canonical Create surface |
| Forge Basilica | `/forge` | Opens the website/coding workspace without restoring a duplicate top-level product |
| Project Dock | `/projects` | Opens saved and resumable project work |
| Archive Canopy | `/library` | Opens the user's Library; no fake public marketplace claim |
| Signal Sail | `/workspace` | Opens the Workspace command surface |

Every City interaction remains prepare → review → confirm. Renderer code does not silently navigate, execute jobs, mutate billing/referral state, or manufacture activity.

## Runtime boundaries preserved

W624C does not change the W624B trust model:

- `/eoncity` remains the only heavy-renderer document.
- `/api/city/access` remains server-authoritative.
- `assets/js/city/eon-city-runtime-owner.js` remains the only production mount/disposal owner.
- The heavy station does not self-mount.
- The eleven-state lifecycle and named-stage progress remain intact.
- Required core and optional detail assets remain separate.
- Optional failure remains degraded-but-usable; required failure remains recoverable/fail-closed.
- Compatibility documents remain static redirects.
- Guest, query-string and LocalStorage state cannot grant heavy access.

## Browser evidence boundary

The included Playwright proof uses the actual `/eoncity` document, the actual Babylon runtime and a loopback-only intercepted response matching the existing access contract. It does not claim production authentication.

The managed environment's installed Chromium rejected both `127.0.0.1` and `localhost` with `ERR_BLOCKED_BY_ADMINISTRATOR`. Playwright's independent Chromium could not be downloaded because outbound DNS was unavailable. Therefore:

- no screenshot was manufactured;
- no WebGL pass was claimed;
- no 9.0/10 visual score was assigned;
- no physical-device, thermal, battery or production-auth proof was claimed.

Run `npm run proof:w624c-command-district:browser` on the owner/Codex machine to produce the missing real-runtime evidence.

## Expansion rule

W624D may improve the avatar and camera inside the existing Command District. Final-quality district expansion remains blocked until the W624C real-runtime score is at least 9.0/10 and no launch-critical category is below 8.5/10.
