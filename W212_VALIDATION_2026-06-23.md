# W212 Validation — 23 June 2026

## Status

**W212 source validation passed.** The result is local source/build validation only. It is not a Cloudflare Preview, D1, browser visual, QR-scanner, physical-device, social-network, or production certificate.

## Passed commands

```text
npm ci --ignore-scripts
npm run qa:w180-w181-chat-first-shell
npm run qa:w184-w187-runtime-market-share
npm run qa:w188-w190
npm run qa:w209-vault-account-boundary
npm run qa:w210-pwa-eonlite-device
npm run qa:w211-workspace-automation
npm run qa:w212-market-links
npm run lint -- --max-warnings=0
npm run build
npm run smoke:build
npm run audit:site
npm run launch:readiness
npm audit --omit=dev
```

## W212 assertions covered

- Default referral links use compact `eon2` self-contained envelopes and a fresh 128-bit nonce.
- Links use the static `/r/#token` shape; no central short-link alias is issued.
- Link verification succeeds without a D1/KV/Worker lookup.
- Legacy `eon1` links remain verifier-compatible; legacy path aliases do not resolve database records.
- The old Cloudflare share-link resolver Functions are absent.
- Referral landing has no central capture on link open.
- Existing D1 tree projection is queued only after a qualifying action and uses a local retry outbox when offline.
- Public share copy does not promise a claim path, AI share campaign, points, money, token, entitlement, or database write for raw sharing.
- Local share attempts retain no signed token fragment.
- Market has only Generate and Official surfaces; Official has no active commerce state; generated previews remain non-minted/non-financial local Vault items.
- Existing W180/W184/W188/W209/W210/W211 source gates remain green.
- Production dependency audit reports 0 vulnerabilities.

## Known open evidence

- Cloudflare Pages Preview checks for `/r/#eon2…`, `/market`, `/vault#referral`, and clean legacy alias retirement.
- A real D1 `REFERRALS_DB` capture only after a deliberately qualified test action, followed by duplicate/outbox retry proof.
- Android QR scan, iPhone Safari link open, desktop browser copy/share, and PWA update persistence.
- Browser/Playwright screenshots and console error capture.
- W213, W214, W215 and W216.
