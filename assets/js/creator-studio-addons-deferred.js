/** W519 — historical distributed-inference add-ons are quarantined.
 * Creator Studio keeps no background provider discovery, P2P transport, earning,
 * or hardware contribution loader in the active browser bundle.
 */
const quarantineNotice = Object.freeze({ enabled: false, reason: 'legacy-transport-control-quarantined' });
void quarantineNotice;
