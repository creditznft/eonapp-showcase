# W624I Codex GitHub and Cloudflare Predeploy

Use the W624I full source snapshot as authoritative. Do not merge from an older W624A–W624H worktree over it.

## One stable certification command

```bash
npm ci
npm run verify:codex-predeploy
```

The command remains lock-protected, source-fingerprinted and resumable. W624I adds one maintained gate without changing the external command name.

It certifies the maintained unit suite, commercial/referral truth, production reachability, W624A–W624I City gates, current-test alignment, historical archive integrity, zero-warning lint, secret scan, production build, smoke and post-build W623F certification.

## GitHub

Apply the complete snapshot, run the stable command until its receipt reports every stage passed, run `git diff --check`, review changed files, commit the complete W624I scope and merge only when PR checks match the packaged receipts.

## Cloudflare

Deploy through the existing project configuration. Do not recreate D1 databases, rotate secrets, rerun destructive migrations or change referral rollout from `testing`. Recheck City guest/authenticated boundaries and existing billing/referral endpoints after deployment.

## Separate evidence lane

```bash
npm run proof:w624i-genuine-agent-theatre:browser
```

This browser fixture cannot replace real Local and Direct BYOK end-to-end proof.
