# W643 real-device evidence

Codex must run the fixed 512×512 batch-1 ComfyUI image baseline on the owner RTX 3050-class 4 GB device, save it, reopen it, add it to Library and continue it into a Project. The same device must show video as blocked without queue submission, download or cloud fallback. Real video can be enabled only after a separate supported device with at least 8 GB measured usable VRAM completes output, save/reopen/playback, cancellation/retry and recovery proof. Evidence must use digests and redacted relative labels, never local absolute paths or model/API secrets.
